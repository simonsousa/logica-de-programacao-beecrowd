var input = require('fs').readFileSync('./stdin', 'utf8');
var lines = input.split('\n').map(Number)
//DIFERENÇA

lines[
    A = lines[0], 
    B = lines[1], 
    C = lines[2], 
    D = lines[3]
]

diferença = (A * B - C * D)

console.log(`DIFERENÇA = ${diferença}`)